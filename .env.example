# ── Vercel Blob ───────────────────────────────────────────────────────────────
# Copy this file to .env and fill in your token.
# Get it from: Vercel Dashboard → your project → Storage → Blob → .env.local tab
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_...

# Optional: override the local dev port (default: 3000)
# PORT=3000
